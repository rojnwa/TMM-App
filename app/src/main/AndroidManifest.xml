<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!-- NICHT WRITE_SMS deklarieren: signature-protected, Default-SMS-Role
         erteilt Schreibrechte am Provider automatisch. Eine WRITE_SMS-uses-permission
         kann auf Android 12+ den ROLE_SMS-Qualifikations-Check brechen. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.RECEIVE_WAP_PUSH" />
    <uses-permission
        android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Tesla zeigt SMS-Sender via Contacts.PhoneLookup. Wir legen pro Mapping
         einen RawContact im App-eigenen Account ab, damit "+888…" → "Anna"
         aufgelöst wird. WRITE_CONTACTS + READ_CONTACTS sind Runtime-Permissions. -->
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- INTERNET + Network-State für den Grok-LLM-Channel. Wenn der User keinen
         API-Key konfiguriert, werden keine Outbound-Calls gemacht. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- BLUETOOTH_CONNECT: erkennt, ob das Handy mit einem der gewählten Teslas
         verbunden ist (verbundene HFP/A2DP-Geräte lesen). Nur dann werden Nachrichten
         weitergeleitet. Optional — ohne Auswahl/Permission greift Fail-Open. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <!-- ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION: aktueller Standort wird als
         Kontext an Grok übermittelt, damit standortbezogene Fragen ("Restaurant in
         der Nähe") korrekt beantwortet werden können. Optional — ohne Permission
         wird kein Standort gesendet; die übrige Funktionalität ist unberührt. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- ACCESS_BACKGROUND_LOCATION: der Grok-Turn läuft ohne UI im Hintergrund
         (Tesla-Reply über den OutboundSmsObserver) — nur mit "Immer erlauben"
         liefert getLastKnownLocation dann Koordinaten. Ab API 30 nicht im
         Runtime-Dialog grantbar; der AssistantScreen führt dafür in die
         App-Einstellungen. Optional wie die Vordergrund-Permissions. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
        <!-- Explizite Sichtbarkeit für Google Messages (smsto-Delegation aus ShellSendActivity) -->
        <package android:name="com.google.android.apps.messaging" />
    </queries>

    <application
        android:name=".MfsApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeslaMessagesManager"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="36">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.TeslaMessagesManager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Tesla OAuth2 Callback-URL -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="io.github.lycheeappf.tmm"
                    android:host="tesla"
                    android:path="/callback" />
            </intent-filter>
        </activity>

        <!-- ==== Default SMS App: Required Components ==== -->

        <!-- 1. SEND/SENDTO Activity — Manifest-Anatomie exakt nach AOSP-Vorlage:
             https://developer.android.com/reference/android/provider/Telephony -->
        <activity
            android:name=".sms.default_app.ShellSendActivity"
            android:enabled="true"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </activity>

        <!-- 2. SMS_DELIVER Receiver -->
        <receiver
            android:name=".sms.default_app.DeliverSmsReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_DELIVER" />
            </intent-filter>
        </receiver>

        <!-- 3. WAP_PUSH_DELIVER Receiver (MMS) -->
        <receiver
            android:name=".sms.default_app.DeliverMmsReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BROADCAST_WAP_PUSH">
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
        </receiver>

        <!-- 4. HeadlessSmsSendService (Respond via Message) -->
        <service
            android:name=".sms.default_app.HeadlessSmsSendService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.SEND_RESPOND_VIA_MESSAGE">
            <intent-filter>
                <action android:name="android.intent.action.RESPOND_VIA_MESSAGE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </service>

        <!-- 5. MMS ContentProvider Stub -->
        <provider
            android:name=".sms.default_app.MmsContentProviderStub"
            android:authorities="${applicationId}.mms"
            android:exported="false"
            android:grantUriPermissions="true" />

        <!-- FileProvider für den Diagnose-Export (content://-Uri fürs Share-Sheet) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ==== Notification Listener Service ==== -->
        <service
            android:name=".listener.NotificationForwardingService"
            android:exported="true"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
            <meta-data
                android:name="android.service.notification.default_filter_types"
                android:value="conversations|alerting" />
        </service>

        <!-- Boot-Receiver für persistente Observer-Wiederherstellung -->
        <receiver
            android:name=".platform.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Tap-Handler für Fallback-Notification (Clipboard-Copy) -->
        <receiver
            android:name=".channel.notification.ClipboardCopyReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="io.github.lycheeappf.tmm.COPY_TO_CLIPBOARD" />
            </intent-filter>
        </receiver>

        <!-- ==== Contacts-Account (für Tesla-Sender-Name-Auflösung) ==== -->
        <service
            android:name=".contact.FakeContactAuthenticatorService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_ACCOUNT_AUTHENTICATOR">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>
            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/account_authenticator" />
        </service>

        <!-- Hilt WorkManager initializer -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
